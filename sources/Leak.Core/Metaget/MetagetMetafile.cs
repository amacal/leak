namespace Leak.Core.Metaget
{
    public class MetagetMetafile
    {
        private readonly MetagetContext context;

        public MetagetMetafile(MetagetContext context)
        {
            this.context = context;
        }
    }
}